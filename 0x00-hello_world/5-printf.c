#include <stdio.h>
/**
 * main - This is a program that prints line with printf function
 * Return:0(efficiency)
 */
int main(void);
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
	return (o);
}
